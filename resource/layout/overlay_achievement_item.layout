"resource/layout/overlay_achievement_item.layout"
{
	controls
	{
		icon { controlname="ImagePanel" scaleimage=1 }
		caption { controlname=label labeltext="#Overlay_Achievement_Recent" }
		name { controlname=label style=name }
	}
	colors
	{
	}
	styles
	{
		name
		{
		textcolor=text
		}
	
	}

	layout
	{
		region { name=body width=max }
		
		place { region=body control=icon y=2 x=0 width=18 height=18 }
		place { region=body control=caption y=0 x=30 width=64 height=64 }
		place { region=body control=name y=68 x=0 width=128 }
	}
}
